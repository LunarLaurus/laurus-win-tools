# Rust Legacy Archive

This folder preserves the original Rust implementation that previously lived at the repository root.

- Status: archived, not the active application
- Current app: [`SoundTracker.App/`](../../SoundTracker.App)
- Migration notes: [`docs/dotnet8-migration.md`](../../docs/dotnet8-migration.md)

Do not treat this directory as a second live codepath. Update it only when preserving historical context or documenting the migration.
