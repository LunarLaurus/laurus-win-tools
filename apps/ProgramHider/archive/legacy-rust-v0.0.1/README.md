# Legacy Rust Prototype

This folder preserves the first Rust-based attempt at `Program Hider`.

Contents kept here:

- original Rust source
- original Rust build script
- the previously packaged Rust executable under `release\v0.1.0`

The active implementation moved to the `.NET` WinForms app in `app/ProgramHider` for faster Windows-specific iteration.
